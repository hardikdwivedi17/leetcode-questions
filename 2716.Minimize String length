class Solution {
    public int minimizedStringLength(String s) {
        int count=0;
        String h="";
    for(int i=0;i<s.length();i++)
    {
        String k=""+s.charAt(i);
        if(!h.contains(k))
        h=h+k;
        

    }
        return h.length();
    }
}
