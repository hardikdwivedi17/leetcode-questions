class Solution {
    public int lengthOfLastWord(String s) {
            int len=0;
        String h = s.trim();
        for(int i=0;i<h.length();i++)
        {
            if(h.charAt(i)==' ')
            len=0;
            else 
            len++;
        }
        return len;
     
    }
}
