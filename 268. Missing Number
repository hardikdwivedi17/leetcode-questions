class Solution {
    public int missingNumber(int[] nums) {
        int len=nums.length;
        int i=0;
        Arrays.sort(nums);
        for(i=0;i<len;i++){
            if(nums[i]!=i)
            return i;
            else 
            continue;
        }
        return i;
    }
}
