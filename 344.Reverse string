class Solution {
    public void reverseString(char[] s) {
    
		int len=s.length;
		char temp;
		char[] a=new char[len];
		for(int i=0;i<s.length;i++)
		{
		    
		    a[i]=s[len-1-i];
		    
		}
		for(int i=0;i<s.length;i++)
		{
		   
		    s[i]=a[i];
		    
		}
    }
}
